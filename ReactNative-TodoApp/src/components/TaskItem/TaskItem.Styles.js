import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        backgroundColor: '#4c9a2a',
        padding: 10,
        borderRadius: 10,
        marginVertical: 10,
    },
    title: {
        color: '#fff',
        fontSize: 18,
    }
})