import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        margin: 10,
    },
    inputContainer: {
        borderWidth: 1,
        padding: 3,
        borderRadius: 5,
        borderColor: '#bdbdbd',
    },
    label: {
        fontSize: 16,
        fontWeight: 'bold',
        marginBottom: 5,
    },
})